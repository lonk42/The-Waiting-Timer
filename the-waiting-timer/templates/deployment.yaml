apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
      - name: the-waiting-timer
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 5000
        env:
        - name: TIMER_NAME
          value: "{{ .Values.timerName }}"
        volumeMounts:
        - name: timer-data
          mountPath: /app/timers.yaml
          subPath: timers.yaml
      volumes:
      - name: timer-data
        persistentVolumeClaim:
          claimName: {{ .Chart.Name }}-pvc
